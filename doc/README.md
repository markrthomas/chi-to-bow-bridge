# Documentation

The generated and source design documentation currently lives under `docs/`.

This `doc/` directory is present to align repository layout with the common Verilog project convention:

- `src/`
- `test/`
- `doc/`
